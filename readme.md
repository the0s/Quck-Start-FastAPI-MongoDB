# Quick Starter with FastAPI and MongoDB

Make sure docker is running!

## Build
docker-compose -f docker-compose.yml build

## Run
docker-compose -f docker-compose.yml up 

## Run the Tests
docker-compose -f docker-compose.yml exec web pytest

